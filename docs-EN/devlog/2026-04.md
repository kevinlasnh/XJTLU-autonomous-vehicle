# April 2026 Development Log

---

## 2026.04.01

### [MPPI Speed Cap Increase + FAST-LIO2 Cloud Pre-Filtering]

- MPPI `vx_max` raised from `0.45` to `0.7 m/s` in two steps (`90d85bf` -> `2471e73`)
- `velocity_smoother.max_velocity[0]` synchronized to `0.7`
- User indoor regression feedback: noticeably faster driving, dynamic obstacle avoidance passed three consecutive times, smoother overall
- FAST-LIO2 now includes publish cloud pre-height-filtering (`f619fa6`):
  - Filters `body_cloud` and `world_cloud` simultaneously at the C++ level (`lio_node.cpp`)
  - Filter window: `publish_cloud_min_z: -0.33` / `publish_cloud_max_z: 0.30` (relative to body origin along gravity)
  - Downstream PGO and Nav2 STVL receive pre-cleaned point clouds; no redundant height filtering needed
  - Parameters managed via `master_params.yaml`; `publish_cloud_height_filter_enabled: true` controls the toggle
- FAST-LIO2 / serial_reader high-frequency logs changed from `RCLCPP_INFO` to `RCLCPP_DEBUG_THROTTLE` / `RCLCPP_INFO_THROTTLE` (5s interval), fixing smoke test stalls caused by log flooding (`3d5de5d`)

### [GPS Corridor Outdoor Regression Test]

- Completed two rounds of outdoor GPS corridor testing on `gps-mppi@3d5de5d`
- Test session: `~/XJTLU-autonomous-vehicle/runtime-data/logs/2026-04-01-11-19-46/`

#### Round 1 Findings

- Startup stable GPS offset (~4.11m) not absorbed into bootstrap; first segment goals systematically shifted
- First waypoint physically offset ~2-3m left, but system judged "reached"
- Waypoint calibration failed: theta delta 176.95deg > 25.00deg (rotation flip)
- Second segment live alignment rejected by guard: translation jump 4.96m > 3.0m threshold
- Runner fell back to first segment's old frozen alignment; second segment goals continued ~5-6m left offset

#### Round 2 Findings

- Second segment subgoal jumped from `(46.03, -30.47)` to `(47.19, -52.68)`; user felt "sudden left turn near the waypoint"
- Late-stage FAST-LIO2/map position drifted left, from `(45.47, -46.96)` down to `(26.44, -63.03)`
- Planner reported `goal off the global costmap` / `worldToMap failed`
- RViz "went blank": most likely fixed-frame viewport not following the vehicle + map drifted out of view, not a node crash
- Rosbag stopped recording early at `11:22:15`; system actually ran until `11:31:13`; second half cannot be replayed

#### User Decision

- Current GPS test round treated as "pass and close"
- All remaining issues recorded in L2, to be fed into next round of design/implementation
- Suggested minimum fixes:
  1. Startup bootstrap should directly absorb the power-on stable GPS translation offset
  2. Waypoint calibration should be translation-only to avoid rotation flips
  3. Alignment refresh granularity should be relaxed from per-waypoint to per-subgoal
