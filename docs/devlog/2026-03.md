# 2026年3月开发日志

---

## 2026.03.16

### [项目管理]
- SSH密钥认证配置（PC→Jetson，ed25519）
- 全面系统扫描Jetson ~/2025_FYP/
- 阅读全部7个开发文档
- 行业调研：Autoware，NVIDIA Isaac ROS，Clearpath，TurtleBot4，Nav2官方推荐
- 设计并通过monorepo重构方案
- Codex执行9阶段迁移计划，Claude验收通过
- Codex额外完成：运行时硬编码路径解耦（16个文件，FYP_RUNTIME_ROOT环境变量）
- Monorepo 68个包构建通过，推送GitHub
- 实车测试SLAM+Explore模式通过
- 文档体系全面重构：按用途分文件，取代按模块分文件

## 2026.03.17

### [GPS Factor 方案 B 部署验证]
- 完成第二条最终方案的软件部署收口验证
- 以太网恢复后，Livox MID360 点云和 IMU 已恢复正常出流
- FAST-LIO2 已确认实际发布 `/fastlio2/lio_odom`
- PGO 已确认实际发布 `/pgo/optimized_odom`
- 当前室内 `/fix` 仍为 `status=-1` 和 `.nan`，属无卫星条件下的预期现象
- `gnss_calibration` no-fix 保护生效：持续跳过无效样本，未生成 `~/fyp_runtime_data/gnss/gnss_offset.txt`
- 当前剩余工作已收敛为：室外有效 GPS fix、`/gnss` 稳定发布、A/B 实车采样、bag/evo 评测

### [系统级优化方案 v3 执行]
- 已按持久停用规则执行系统服务清理，并完成一次 Jetson 重启验证
- 重启后保持关闭: runsunloginclient, bluetooth, ModemManager, kerneloops
- 重启后保持 masked: snapd, avahi-daemon, rpcbind, packagekit
- snap cups 服务已通过 `snap stop --disable` 持久停用
- 保留项正常: gdm3, ToDesk, Clash
- `~/.ros/log` 已从约 2.6G 清理到约 76M

### [统一参数管理方案落地]
- 新建 `feature/unified-params` 分支，开始统一参数管理改造
- `fastlio2` / `pgo` 已改为“ROS2 原生参数 + legacy config_path 兼容回退”双轨模式
- 新增 `src/bringup/config/master_params.yaml`，统一收口 FAST-LIO2、PGO、串口、GNSS、pointcloud_to_laserscan 参数
- `system_explore` / `system_explore_gps` / `system_slam` / `system_travel` 现已通过 launch 显式传递 master params
- `gnss_calibration_launch.py` / `nmea_serial_driver.launch.py` 已补齐 params_file 传递链
- 构建通过：`fastlio2`、`pgo`、`bringup`、`nmea_navsat_driver`、`gnss_calibration`
- 参数验证通过：
  - `/fastlio2/lio_node lidar_max_range = 15.0`
  - `/pgo/pgo_node gps.noise_xy = 2.5`
  - `/pgo/pgo_node gps.topic = /gnss`
  - `pointcloud_to_laserscan range_max = 30.0`
  - `pointcloud_to_laserscan queue_size = 10`
- legacy 兼容验证通过：`pgo_launch.py pgo_config:=pgo_no_gps.yaml` 时，`/pgo/pgo_node gps.enable = false`
- GNSS 链实测使用 master params 成功起驱动，`/fix.header.frame_id = gps`，室内无卫星时 `gnss_calibration` 继续按预期跳过 no-fix 样本

### [统一日志方案落地]
- 新建 `feature/unified-logs` 分支，开始统一日志管理改造
- 新增 `scripts/launch_with_logs.sh`，统一创建 `console/`、`data/`、`system/` 三层 session 日志目录
- `Makefile` 的 `launch-slam` / `launch-explore` / `launch-explore-gps` / `launch-travel` 已切到 wrapper
- 已修改 9 个节点的自定义日志路径，统一支持 `FYP_LOG_SESSION_DIR`
- Livox 保留双日志语义：`livox_imu.log` + `livox_pointcloud.log`
- `wheeltec_gps_path_std.txt` 已纳入 session 隔离，同时保留 direct launch 的旧路径 fallback
- 实际部署中发现 `set -u` 会与 ROS 2 `setup.bash` 冲突，wrapper 已修正为 source 前临时 `set +u`
- 构建通过：`fastlio2`、`pgo`、`serial_reader`、`serial_twistctl`、`livox_ros_driver2`、`wheeltec_gps_path`、`wit_ros2_imu`、`nmea_navsat_driver`、`gnss_calibration`、`bringup`
- wrapper smoke test 通过：
  - `~/fyp_runtime_data/logs/latest/console/` 已生成 ROS2 console log
  - `~/fyp_runtime_data/logs/latest/data/` 已生成自定义数据日志（按 `log_switch.yaml` 开关）
  - `~/fyp_runtime_data/logs/latest/system/` 已生成 `tegrastats.log` 和 `session_info.yaml`
  - `session_info.yaml` 已正常写入 `start_time` / `end_time`
  - `tegrastats` 没有残留进程
- direct `ros2 launch` fallback 已验证：`logs/twist_log/` 文件数从 `12` 增加到 `13`，旧路径兼容链正常
