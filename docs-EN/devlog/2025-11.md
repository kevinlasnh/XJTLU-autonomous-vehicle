# November 2025 Development Log

---

## 2025.11.10

### [Project Management]
- Wrote standardized development documentation and memos for the entire project

### [Sensor Layer]
- Resolved the issue where the LiDAR node could not write data to log while outputting point cloud data, by modifying the specific function in the LiDAR driver file

### [Nav2]
- Analyzed the third unresolved Nav2 issue (costmap disappearing), found it was caused by FAST-LIO output dropout

---

## 2025.11.11

### [Project Management]
- Investigated the issue where the self-built map rotates with the vehicle body rotation; identified as a `fix_framed` parameter setting issue in rviz2

### [Sensor Layer]
- Changed LiDAR message timestamps to use system time
- Confirmed that WIT IMU message publish timestamps are determined by system time; changed log timestamps to nanosecond format
- Confirmed that serial reader message publish timestamps are determined by system time
- Changed nmea and wheeltec log timestamps to ROS standard timestamps
- Added log timestamp writing functionality to the serial output node `serial_twistctl`
- Added log enable/disable parameter detection to every node capable of producing logs; created a unified YAML management system

### [Perception Layer]
- Added log file writing functionality to FAST-LIO and PGO nodes
- Increased PGO message queue depth to 50, resolving message loss during operation
- Observed that GPU usage accumulates as runtime increases
- PGO produces `Message Filter dropping message` errors during long-running sessions

### [Nav2]
- Discovered that increasing costmap update/publish frequency causes maps to stop publishing; adjusted local frequency to 50Hz (using double type 50.0)
- Global costmap update speed meets requirements without sticking, but local still sticks

---

## 2025.11.12

### [Project Management]
- Confirmed the map-rotating-with-vehicle issue as a `fix_framed` parameter setting issue in rviz2

---

## 2025.11.13

### [Project Management]
- Vehicle hardware wiring may be loose; timestamp alignment needed; PID emergency stop reversal issue; updated project file classification

### [Sensor Layer]
- Outdoor GPS data is normal, accurate to 0.1 meters, approximately 27 satellites
- Camera unusable (USB 2.0 limitation)
- Attempted C board odometry closed-loop control, causing the vehicle to go haywire

### [Perception Layer]
- Updated FAST-LIO2 execution frequency to 50Hz
- Reduced PGO publish frequency to 20Hz

### [Nav2]
- Unified obstacle layer usage to resolve map disappearing issue
- System CPU/GPU maintained at 60%
- After long runtime, vehicle stops but path and costmap continue updating

---

## 2025.11.14

### [Nav2]
- Can attempt to enable more Nav2 features

---

## 2025.11.17

### [Nav2]
- Parameter tuning protocol: one parameter at a time, with documentation
- `robot_radius` calculation: front 500mm, side 650mm, rotation radius 386.25mm
- Added comments to `control_server` parameters
- Adjusted maximum travel speed to 0.5m/s
- Enabled reverse driving (`min_vel_x=-0.5`)
- Set `max_vel_theta` to 1.0 rad/s
- Adjusted `acc_lim_theta=3.5`, `acc_lim_x=3.0`, with corresponding decel values
- Synchronized `velocity_smoother` `max_accel` and `max_decel`
- Closed-loop control not yet enabled (OPEN_LOOP); unified `odom_topic` to `/fastlio2/lio_odom`
- `behavior_server rotational_acc_lim=3.5`
- `cost_scaling_factor=2.5`, `resolution=0.02m`
- Found vehicle motion not matching expectations; obstacle residue issues; vehicle drives in reverse after enabling reverse
- Adjusted DWB: `BaseObstacle.scale=0.002` (later found too low), `RotateToGoal.scale=200`, `GoalAlign.scale=200`
- Discovered global map rolling window causing paths through obstacles
- Long-runtime stopping issue persists

---

## 2025.11.18

### [Perception Layer]
- Added new FASTLIO2 development conversation session in Copilot
- Need to review the localizer package

### [Nav2]
- Added PreferForward to penalize reverse trajectories
- Set up static map ecosystem; created new travel YAML file
- PGO mode Nav2 launch file updated accordingly

---

## 2025.11.19-21

### [Project Management]
- Working on coursework, development paused

---

## 2025.11.22

### [Sensor Layer]
- Found GPS module obstruction affecting costmap

### [Perception Layer]
- (No separate entry)

### [Nav2]
- Tested reverse driving, unsatisfactory results; disabled reverse (`min_vel_x=0`)
- Obstacle avoidance suddenly broken; corrected `BaseObstacle.scale` typo from 0.002 back to 0.02
- Reduced `trans_stopped_velocity` to 0.05
- `PathAlign+GoalAlign forward_point_distance=0.325`
- Reduced `GoalAlign.scale` to 16.0
- Enabled CLOSE_LOOP with C board odometry -> vehicle spun wildly; switched to FAST-LIO2 -> vehicle stopped following path
- Ceiling obstacle false detection resolved by lowering max obstacle height to 1.5m
- Closed-loop control not enabled for now; open-loop obstacle avoidance is acceptable

### [Launch]
- Confirmed YAML parameters updated in launch files
- Added serial reader node
- Added new Nav2 launch file

---

## 2025.11.23

### [Perception Layer]
- LiDAR data degrades when too close to walls
- Adjusted PGO keyframe capture parameters
- Successfully scanned corridor map
- SLAM toolbox QoS configuration
- Successfully generated 2D occupancy grid map
- SLAM toolbox runs out of memory during extended mapping
- Analysis conclusion: slam_toolbox cannot replace the PGO node

### [Nav2]
- Preparing to scan 2D occupancy grid map
- Modified launch file to add `mapping_mode` parameter
- Created `system_entire_launch_travel.py`
- Connected `/scan` topic to Nav2

---

## 2025.11.24

### [Sensor Layer]
- Attempted C board odometry closed-loop control again; vehicle went haywire; data source unclear

---

## 2025.11.25

### [Project Management]
- Re-checked console workspace configuration; CMake modification rules
- Key development insight: GPS global path -> segment into waypoints -> GPS to Nav2 target pose conversion -> Nav2 only handles point-to-point navigation
- Detailed GPS-Nav2 conversion scheme design (11 detailed reasoning points)
- Nav2 natively does not support single-costmap navigation; requires source code modification

### [Nav2]
- Still using FAST-LIO2 output
- Decided against developing pre-loaded local costmap
- `movement_time_allowance=3.0`
- Developed `waypoint_collector` intermediate node -> tested successfully
- Modified costmap: removed global costmap obstacle layer -> planner crashed -> plugin array cannot be empty -> rolled back

---

## 2025.11.26

### [Project Management]
- Attempted converting all GPS waypoints to Nav2 waypoints at once (using `waypoint_follower`); concerned about memory issues
- Project milestone summary: listed 16 issues + 2 development modules

---

## 2025.11.27

### [Project Management]
- Development needs to be modular; documentation needs standardization

### [Global Planning]
- Created Global_doc.md
- A* algorithm feature points need extraction

### [Perception Layer]
- FAST-LIO2 rviz configuration modified
- Tri-fusion algorithm coordinate calibration considerations (10 reasoning points on long-distance GPS calibration)

---

## 2025.11.28

### [Perception Layer]
- Studied odometry data computation
- Summarized fusion algorithm approach
- Rviz Decay time explanation
- ikd-tree explanation
- Difference between body_cloud and world_cloud
- Incremental map built by PGO's `m_key_poses[]`

---

## 2025.11.30

### [Perception Layer]
- PGO drift issue without loop closure over long distances
- Loop closure correction does not necessarily match physical scene
- Completed full algorithm analysis for FAST-LIO2 + PGO
- Partial algorithm pipeline built for tri-fusion GPS component
